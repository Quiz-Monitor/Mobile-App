class ExamModel {
  final String title, prof;
  final bool isLive;
  final DateTime dateTime;

  ExamModel({
    required this.title,
    required this.prof,
    required this.isLive,
    required this.dateTime,
  });
}
